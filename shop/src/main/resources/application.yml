shop:
  schedule:
    update-statistics-every-hour: 1

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5431/shop
    username: shop
    password: shop

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate

server:
  error:
    include-message: always
cxf:
  path: /soap
